# 健身 APP 开发接续说明

更新时间：2026-09-04

## 真源与发布边界

- 当前检出的仓库是唯一代码开发源；不要在另一个本地目录同时修改源码。
- `origin/main` 是公开部署分支，GitHub Pages 通过 Actions 构建。
- 用户试用域名仍记录为：https://app.yuchengmini.cn/ 。本轮只完成仓库与构建验收，没有重新核验线上域名、Supabase 数据或外部邮箱链路。
- 私人健身记录、历史 QA 截图、本地研究和 Agent 临时状态不属于公开仓库。

## 当前可核验状态

- 本地 `main` 已快进到远端提交 `8952e5dbbececc260a862cc1282538f979dd4db4`。
- 已在全新克隆中执行 `npm ci` 和 `npm run verify:maintenance`。
- 应用逻辑、周期记录、自适应训练、浏览器冒烟、维护差异检查和生产构建全部通过。
- Vite 主 JavaScript 包约 541.34 kB，仍有超过 500 kB 的体积警告；该警告不阻塞当前构建。
- 常见密钥形态扫描未在现有 46 个 Git 提交中发现命中。

## 已有能力边界

- 新手计划、周期状态、自适应训练、训练资料、身体趋势和动作记录已有本地逻辑与回归测试。
- 周期、训练资料、身体趋势和动作历史按测试约束保持本地，不进入通用云同步快照。
- 现有测试通过不等于线上服务、真实邮箱、Supabase 权限或移动设备在 2026-09-04 仍已实页验证。
- 不把健身建议表述为医疗、康复或诊断结论。

## 下次第一步

1. 执行 `git status --short --branch`，确认没有私人资料进入暂存区。
2. 如果本地没有依赖，运行 `npm ci`；随后运行 `npm run verify:maintenance`。
3. 再单独核验用户试用域名、注册登录和反馈链路的当前状态，失败时记录“无法验证”，不要沿用 2026-07 的旧结论。
4. 线上链路通过后，再按 `docs/pilot/2026-07-11-friends-round-1.md` 决定是否继续小范围试用。
5. 只根据真实试用中的高频阻塞点改动，不先扩展大功能。

## 本地注意事项

- `.env.local`、`.env.*.local`、`.superpowers/`、`node_modules/`、`dist/` 和浏览器验收输出均不得提交。
- 私人记录与历史验收资料由 Vault 外层入口管理，公开仓库只保留产品代码、必要技术文档和安全的公开素材。
- 任何涉及身体数据、周期信息或真实用户反馈的文件，在提交前必须单独检查。
